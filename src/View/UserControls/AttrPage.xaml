<UserControl x:Class="PilotLookUp.View.UserControls.AttrPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:PilotLookUp.View.UserControls"
             xmlns:utils="clr-namespace:PilotLookUp.Enums"
             xmlns:resx="clr-namespace:PilotLookUp.Resources"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">

    <!-- Экспорт словарей ресурсов Pilot -->
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="..\..\Resources\StyleDictionary.xaml" />
                <ResourceDictionary Source="..\..\Resources\VMMap.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="40" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Grid Background="{DynamicResource HoverBackground}">
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="5,0,0,0">
                <TextBlock Text="{x:Static resx:Strings.SelectedElementId}" FontWeight="Bold" />
                <TextBlock Text="{Binding IdSelectedItem}" Margin="5,0" />
                <TextBlock Text="{x:Static resx:Strings.NameLabel}" FontWeight="Bold" Margin="20,0,0,0" />
                <TextBlock Text="{Binding NameSelectedItem}" Margin="5,0" />
            </StackPanel>
        </Grid>

        <Grid Grid.Row="1">
            <DataGrid ItemsSource="{Binding Attrs}"
           Grid.Column="2"
           IsReadOnly="True"
           SelectedItem="{Binding DataGridSelected}">

                <DataGrid.ContextMenu>
                <ContextMenu>
                    <MenuItem Header="{x:Static resx:Strings.CopyName}"
                       Command="{Binding CopyCommand}"
                       CommandParameter="{x:Static utils:CopyCommandKey.DataGridSelectName}" />
                    <MenuItem Header="{x:Static resx:Strings.CopyValue}"
                       Command="{Binding CopyCommand}"
                       CommandParameter="{x:Static utils:CopyCommandKey.DataGridSelectValue}" />
                    <MenuItem Header="{x:Static resx:Strings.CopyTitle}"
                       Command="{Binding CopyCommand}"
                       CommandParameter="{x:Static utils:CopyCommandKey.DataGridSelectTitle}" />
                </ContextMenu>
            </DataGrid.ContextMenu>

                <DataGrid.Columns>
                    <DataGridTextColumn Header="{x:Static resx:Strings.GridColumnName}"
                                            Binding="{Binding Name, Mode=OneWay, UpdateSourceTrigger=PropertyChanged}"
                                            Width="*" />

                    <DataGridTextColumn Header="{x:Static resx:Strings.GridColumnTitle}"
                                            Binding="{Binding Title, Mode=OneWay, UpdateSourceTrigger=PropertyChanged}"
                                            Width="*" />

                    <DataGridTextColumn Header="{x:Static resx:Strings.GridColumnValue}"
                                            Binding="{Binding Value, Mode=OneWay, UpdateSourceTrigger=PropertyChanged}"
                                            Width="*" />

                    <DataGridTextColumn Header="{x:Static resx:Strings.GridColumnIsObligatory}"
                                            Binding="{Binding IsObligatory, Mode=OneWay, UpdateSourceTrigger=PropertyChanged}"
                                            Width="*" />

                    <DataGridTextColumn Header="{x:Static resx:Strings.GridColumnIsService}"
                                            Binding="{Binding IsService, Mode=OneWay, UpdateSourceTrigger=PropertyChanged}"
                                            Width="*" />

                    <DataGridTextColumn Header="{x:Static resx:Strings.GridColumnType}"
                                            Binding="{Binding Type, Mode=OneWay, UpdateSourceTrigger=PropertyChanged}"
                                            Width="*" />

                    <DataGridTextColumn Header="{x:Static resx:Strings.GridColumnIsInitialized}"
                                            Binding="{Binding IsInitialized, Mode=OneWay, UpdateSourceTrigger=PropertyChanged}"
                                            Width="*" />
                    
                    <DataGridTextColumn Header="{x:Static resx:Strings.GridColumnIsValid}"
                                            Binding="{Binding IsValid, Mode=OneWay, UpdateSourceTrigger=PropertyChanged}"
                                            Width="*" />
                </DataGrid.Columns>
            </DataGrid>
        </Grid>
    </Grid>
</UserControl>
